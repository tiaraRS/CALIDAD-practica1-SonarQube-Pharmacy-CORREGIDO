# must be unique in a given SonarQube instance
sonar.projectKey=practica1-pharmacy-corregido

# --- optional properties ---

# defaults to project key
sonar.projectName=practica1-pharmacy-corregido
# defaults to 'not provided'
#sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=.
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
sonar.host.url=http://localhost:9000

sonar.login=a18625d2d6716ab0fffb542a6906db664f2d0751

#We did not set language because we don't want to limit the analysis to only one language, our project involves py, js, etc.
#We found that is not neccesary specify the language in this new version
#sonar.language=
